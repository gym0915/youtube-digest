# 容量受限的视频缓存采用本地存储与最近访问淘汰

状态：accepted

视频缓存继续保存在 `chrome.storage.local`，不申请 `unlimitedStorage`，并以 6 MiB 作为 Transcript、Overview 与两者翻译结果的共同预算。缓存命中优先直接展示，普通使用不刷新；达到预算时按最近访问时间淘汰整条视频缓存，既有缓存以原写入时间平滑迁移且不在升级时主动删除。Notes、Notes 翻译和一次性讲解不属于视频缓存，清除视频缓存也不影响它们；若删完可淘汰的视频缓存仍无法写入，放弃新增持久缓存，绝不删除用户笔记、设置或密钥。
