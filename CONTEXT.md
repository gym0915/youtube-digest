# YouTube Digest 翻译语境

本语境用于定义 YouTube Digest Transcript、Overview 与 Notes 内容翻译功能中的核心业务术语与边界，确保原文、中文、双语和渐进式翻译对同一概念使用一致语言。

## 术语

### 内容与结果

**Transcript**：视频中作为翻译与 Overview 共同输入的字幕内容，由带时间信息的原始字幕片段组成。
_避免使用_：视频脚本、字幕行（指完整字幕内容时）

**Transcript 版本**：某个视频、字幕来源和源语言对应的一次确定内容状态；字幕内容被修订、来源改变或源语言改变时，形成不同版本。
_避免使用_：视频版本、缓存版本（两者不能准确表达字幕内容身份）

**Overview**：基于 Transcript 生成的视频内容结构化概览，包含章节标题、章节摘要和重点引语，帮助用户理解主题与重点。
_避免使用_：摘要（过于宽泛，无法与其他摘要结果区分）

**内容翻译模式**：作用于 Transcript、Overview 和 Notes 的统一内容展示选择，包括原文、中文和双语；它不等同于 Settings 的界面语言。
_避免使用_：字幕模式（范围只覆盖 Transcript）、界面语言（描述产品文案语言）

**内容项**：可独立展示和翻译的业务内容单元，包括 Transcript 语义段、Overview 的章节字段或重点引语，以及一条 Note 的正文。
_避免使用_：文本块（无法表达内容所属的业务表面）

**翻译结果**：针对一个可翻译内容版本和目标语言生成的内容项译文。
_避免使用_：译文（未说明其对应的 Transcript 版本和目标语言时）

**生成配置**：决定 Overview 或翻译结果内容的模型、提示词/模板和输出约束的组合。
_避免使用_：模型配置（无法表达提示词和输出约束的变化）

**语义段**：由一个或多个原始字幕片段组成、适合独立翻译和阅读的完整语义单元。
_避免使用_：原始字幕行（指语义段时）、句子（未说明字幕聚合边界时）

**可见翻译窗口**：当前内容表面的阅读视口及其紧邻预取范围组成的翻译优先区域；只有进入该区域的内容项才获得新的翻译资格。
_避免使用_：全文翻译区、后台翻译区

**翻译会话**：围绕当前视频、内容翻译模式和当前内容表面阅读上下文进行的一组连续翻译工作；阅读上下文离开当前表面或发生切换时，尚未发送的工作结束。
_避免使用_：全局翻译任务、翻译进度（泛指局部工作时）

**译文缓存**：按内容项身份、内容版本和目标语言保存的翻译结果；中文模式与双语模式共同读取它，不因展示模式不同而重复生成。
_避免使用_：中文模式缓存、双语缓存、全文译文

**缓存命中**：请求找到与目标内容、语言和生成配置相匹配的完整缓存结果。
_避免使用_：缓存存在（存在不代表身份匹配或结果完整）

**陈旧结果**：曾经完整且可用、但已超过当前新鲜度策略的缓存结果；它可以用于展示或刷新失败时回退，但不视为最新结果。
_避免使用_：失效数据（陈旧不等于不可用）

**缓存作用域**：允许复用某项缓存结果的用户与浏览上下文范围；第一阶段以同一浏览器配置为共享边界，公开视频结果可在该边界内复用，私人内容不跨越该边界。
_避免使用_：全局缓存（会误解为跨设备、跨用户共享）

**语义段算法版本**：将 Transcript 聚合为语义段时所采用的一组规则版本；规则变化可能改变语义段边界和翻译结果的归属。
_避免使用_：Transcript 版本（它描述字幕内容，不描述分段规则）

**缓存刷新**：为重新获取 Transcript 或重新生成派生结果而发起的过程；当前产品不提供该操作，普通缓存命中也不会在后台自动触发它。
_避免使用_：重新加载（无法区分界面加载与内容刷新）

**回退结果**：刷新或生成失败时继续提供的、此前完整可用的结果；它可能是陈旧结果，但不会被当作最新结果。
_避免使用_：失败结果（回退结果本身不是失败产物）

**缓存淘汰**：本地缓存接近容量边界时，移除最久未访问的一整条视频缓存的过程；它不因单纯经过一段时间而删除，也不同于用户主动删除。
_避免使用_：缓存清理（容易与用户发起的全量清除混淆）

**视频缓存预算**：在 Chrome 本地存储中为全部视频缓存预留的最大空间；当前为 8 MiB，独立于并低于浏览器可用总空间，剩余空间供设置、Notes 和 Notes 翻译结果使用。
_避免使用_：Chrome 配额、视频数量上限（前者不是产品可支配的全部空间，后者无法约束实际大小）

**缓存访问**：一次成功读取视频缓存并使其成为当前阅读内容的事件；它更新该视频缓存的最近访问时间，用于后续缓存淘汰排序。
_避免使用_：缓存写入、刷新时间（两者不能代表用户实际复习过内容）

**缓存迁移**：将既有缓存纳入新缓存规则而不强制删除或重新获取的过程；没有访问记录的旧缓存以原写入时间作为首次淘汰排序依据。
_避免使用_：缓存重建、缓存清空（两者都会破坏仍可读的既有结果）

**缓存写入拒绝**：新增视频缓存因可用空间不足而未被持久保存的结果；当前阅读和翻译仍可继续，但系统不会为腾空间删除 Notes、Notes 翻译、设置或 API Key。
_避免使用_：数据清理、存储重置（它不删除已有用户数据）

**视频缓存**：归属于一个视频、可在同一 Chrome 配置中复用的 Transcript、Overview 与两者翻译结果；Notes 和选中文本讲解不属于它。
_避免使用_：全部本地数据、视频资料库（会混入独立的用户笔记或一次性结果）

**清除视频缓存**：用户主动删除全部视频缓存的本地数据操作；它不删除 Notes 或 Notes 的翻译结果。
_避免使用_：重置数据、删除学习记录（会扩大到无关的用户数据）

**不保留视频**：用户对某个视频设置的保留例外，表示该视频的 Transcript、翻译结果、Overview 和关联 Notes 只在当前会话中使用，不写入持久缓存。
_避免使用_：私密视频（视频的公开状态未必能被扩展可靠判断）

**过期翻译响应**：返回时已不再属于当前视频、内容翻译模式或翻译会话的翻译结果；它不能更新当前内容表面的界面，但在能安全归属原内容时可以补充译文缓存。
_避免使用_：翻译失败（上下文失效不等于请求失败）
